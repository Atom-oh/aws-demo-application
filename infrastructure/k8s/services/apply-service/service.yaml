apiVersion: v1
kind: Service
metadata:
  name: apply-service
  namespace: hirehub
  labels:
    app.kubernetes.io/name: apply-service
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: apply-service
  ports:
    - name: grpc
      port: 50053
      targetPort: grpc
      protocol: TCP
    - name: http
      port: 8004
      targetPort: http
      protocol: TCP
    - name: metrics
      port: 9090
      targetPort: metrics
      protocol: TCP
---
# Headless service for gRPC load balancing
apiVersion: v1
kind: Service
metadata:
  name: apply-service-headless
  namespace: hirehub
  labels:
    app.kubernetes.io/name: apply-service
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app.kubernetes.io/name: apply-service
  ports:
    - name: grpc
      port: 50053
      targetPort: grpc
      protocol: TCP
