apiVersion: apps/v1
kind: Deployment
metadata:
  name: apply-service
  namespace: hirehub
  labels:
    app.kubernetes.io/name: apply-service
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: hirehub-platform
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: apply-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: apply-service
        app.kubernetes.io/component: backend
        network-policy/internal: "true"
        network-policy/ingress: "kong"
        network-policy/aws-access: "true"
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9090"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: hirehub-service-account
      terminationGracePeriodSeconds: 30
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app.kubernetes.io/name: apply-service
                topologyKey: kubernetes.io/hostname
            - weight: 50
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app.kubernetes.io/name: apply-service
                topologyKey: topology.kubernetes.io/zone
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: apply-service
      containers:
        - name: apply-service
          image: hirehub/apply-service:latest
          imagePullPolicy: Always
          ports:
            - name: grpc
              containerPort: 50053
              protocol: TCP
            - name: http
              containerPort: 8004
              protocol: TCP
            - name: metrics
              containerPort: 9090
              protocol: TCP
          envFrom:
            - configMapRef:
                name: apply-service-config
          env:
            - name: DB_HOST
              valueFrom:
                secretKeyRef:
                  name: apply-service-secrets
                  key: DB_HOST
            - name: DB_PORT
              valueFrom:
                secretKeyRef:
                  name: apply-service-secrets
                  key: DB_PORT
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: apply-service-secrets
                  key: DB_USERNAME
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: apply-service-secrets
                  key: DB_PASSWORD
            - name: KAFKA_BROKERS
              valueFrom:
                secretKeyRef:
                  name: hirehub-kafka-credentials
                  key: KAFKA_BROKERS
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            grpc:
              port: 50053
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            grpc:
              port: 50053
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          startupProbe:
            grpc:
              port: 50053
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 30
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            runAsUser: 1000
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
