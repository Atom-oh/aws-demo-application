{{- if and .Values.enabled .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "job-service.serviceAccountName" . }}
  namespace: {{ include "job-service.namespace" . }}
  labels:
    {{- include "job-service.labels" . | nindent 4 }}
  annotations:
    {{- include "job-service.irsaAnnotation" . | nindent 4 }}
    {{- with .Values.serviceAccount.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
{{- end }}
