{{- if and .Values.enabled .Values.serviceMonitor.enabled }}
{{- if .Values.global }}
{{- if .Values.global.monitoring }}
{{- if .Values.global.monitoring.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "match-service.fullname" . }}
  namespace: {{ include "match-service.namespace" . }}
  labels:
    {{- include "match-service.labels" . | nindent 4 }}
    release: prometheus
spec:
  selector:
    matchLabels:
      {{- include "match-service.selectorLabels" . | nindent 6 }}
  namespaceSelector:
    matchNames:
      - {{ include "match-service.namespace" . }}
  endpoints:
    - port: http
      path: {{ .Values.serviceMonitor.path }}
      interval: {{ .Values.serviceMonitor.interval }}
      scrapeTimeout: {{ .Values.serviceMonitor.scrapeTimeout }}
      honorLabels: true
{{- end }}
{{- end }}
{{- end }}
{{- end }}
