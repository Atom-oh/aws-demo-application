{{- if and .Values.enabled .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "match-service.serviceAccountName" . }}
  namespace: {{ include "match-service.namespace" . }}
  labels:
    {{- include "match-service.labels" . | nindent 4 }}
  annotations:
    {{- include "match-service.irsaAnnotation" . | nindent 4 }}
    {{- with .Values.serviceAccount.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
{{- end }}
