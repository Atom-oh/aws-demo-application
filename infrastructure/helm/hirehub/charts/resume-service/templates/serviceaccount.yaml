{{- if and .Values.enabled .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "resume-service.serviceAccountName" . }}
  namespace: {{ include "resume-service.namespace" . }}
  labels:
    {{- include "resume-service.labels" . | nindent 4 }}
  annotations:
    {{- include "resume-service.irsaAnnotation" . | nindent 4 }}
    {{- with .Values.serviceAccount.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
{{- end }}
