{{- if and .Values.enabled .Values.serviceMonitor.enabled }}
{{- if .Values.global }}
{{- if .Values.global.monitoring }}
{{- if .Values.global.monitoring.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "resume-service.fullname" . }}
  namespace: {{ include "resume-service.namespace" . }}
  labels:
    {{- include "resume-service.labels" . | nindent 4 }}
    release: prometheus
spec:
  selector:
    matchLabels:
      {{- include "resume-service.selectorLabels" . | nindent 6 }}
  namespaceSelector:
    matchNames:
      - {{ include "resume-service.namespace" . }}
  endpoints:
    - port: http
      path: {{ .Values.serviceMonitor.path }}
      interval: {{ .Values.serviceMonitor.interval }}
      scrapeTimeout: {{ .Values.serviceMonitor.scrapeTimeout }}
      honorLabels: true
{{- end }}
{{- end }}
{{- end }}
{{- end }}
