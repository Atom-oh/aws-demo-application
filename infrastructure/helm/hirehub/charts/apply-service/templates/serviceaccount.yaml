{{- if and .Values.enabled .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "apply-service.serviceAccountName" . }}
  namespace: {{ include "apply-service.namespace" . }}
  labels:
    {{- include "apply-service.labels" . | nindent 4 }}
  annotations:
    {{- include "apply-service.irsaAnnotation" . | nindent 4 }}
    {{- with .Values.serviceAccount.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
{{- end }}
