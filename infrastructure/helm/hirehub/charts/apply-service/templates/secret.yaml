{{- if .Values.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "apply-service.fullname" . }}-secret
  namespace: {{ include "apply-service.namespace" . }}
  labels:
    {{- include "apply-service.labels" . | nindent 4 }}
type: Opaque
data:
  {{- if .Values.secrets.dbPassword }}
  DB_PASSWORD: {{ .Values.secrets.dbPassword | b64enc | quote }}
  {{- else }}
  DB_PASSWORD: {{ "changeme" | b64enc | quote }}
  {{- end }}
stringData:
  DB_NAME: "hirehub_applications"
  DB_USER: "apply_service"
